import React, { PureComponent, Fragment} from 'react';

export default class Writter extends PureComponent{
    render(){
        return(
            <Fragment>
                <div>Writter~</div>
            </Fragment>
        )
    }
}